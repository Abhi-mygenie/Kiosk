// Theme Index - Export all theme values
import { colors } from './colors';
import { fonts, fontSizes, lineHeights } from './typography';
import { spacing, borderRadius } from './spacing';

export const theme = {
  colors,
  fonts,
  fontSizes,
  lineHeights,
  spacing,
  borderRadius,
};

export { colors, fonts, fontSizes, lineHeights, spacing, borderRadius };
export default theme;
